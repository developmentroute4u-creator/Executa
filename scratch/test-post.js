const fetch = require("node-fetch"); // Wait, we can use global fetch if node version is >= 18

async function run() {
  // Let's call the POST api by mocking headers or let's inspect the API route code to see why it fails
  console.log("No-op");
}

run();
